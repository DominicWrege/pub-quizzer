{
  "name": "pub-quizzer-e2e",
  "private": true,
  "description": "Playwright E2E tests for the PubQuizzer frontend. Distinct from assets/package.json, which holds bundled frontend deps (Tailwind/daisyUI).",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "codegen": "playwright codegen http://localhost:4000",
    "show-report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
